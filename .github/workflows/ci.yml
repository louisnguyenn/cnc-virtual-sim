name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1 — check out the repo
      - name: Checkout
        uses: actions/checkout@v4

      # 2 — install dependencies
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            python3 \
            python3-pip

      # 3 — configure with cmake
      - name: Configure
        run: |
          cmake -B build \
                -G Ninja \
                -DCMAKE_BUILD_TYPE=Debug

      # 4 — build
      - name: Build
        run: cmake --build build

      # 5 — run tests
      - name: Test
        run: |
          cd ${{ github.workspace }}
          ./build/tests/cnc_tests
  # format checking job
  format-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install clang-format
        run: sudo apt-get install -y clang-format

      - name: Check formatting
        run: |
          find src include tests -name "*.cpp" -o -name "*.h" | \
              xargs clang-format --dry-run --Werror
  clang-tidy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential cmake ninja-build clang clang-tidy

      - name: Configure
        run: |
          cmake -B build -G Ninja \
                -DCMAKE_BUILD_TYPE=Debug \
                -DCMAKE_CXX_COMPILER=clang++

      - name: Build
        run: cmake --build build

      - name: Run clang-tidy
        run: |
          clang-tidy -p build \
            src/parser/GcodeParser.cpp \
            src/motion/MotionEngine.cpp \
            src/motion/MachineConfig.cpp \
            src/simulator/Simulator.cpp \
            src/simulator/AppConfig.cpp \
            src/logger/Logger.cpp \
            src/logger/AppLogger.cpp